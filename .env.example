# Скопируй в .env. Локально — SQLite (нулевая настройка).
DATABASE_URL="file:./dev.db"

# Для прода — Postgres (и provider = "postgresql" в schema.prisma):
# DATABASE_URL="postgresql://user:pass@host:5432/lapka?schema=public"

# Яндекс.Карты (необязательно). Без ключа карта работает списком меток:
# NEXT_PUBLIC_YANDEX_MAPS_KEY="ваш-ключ"
